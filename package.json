{
  "name": "north-bharat-jobs",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute north_bharat_jobs --remote --file=schema.sql",
    "db:local": "wrangler d1 execute north_bharat_jobs --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}